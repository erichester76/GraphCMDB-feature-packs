{
  "Azure_Subscription": {
    "display_name": "Azure Subscription",
    "category": "Cloud",
    "description": "A Microsoft Azure subscription, the root container for resources.",
    "properties": ["name", "subscription_id", "tenant_id"],
    "required": ["name", "subscription_id"],
    "relationships": {}
  },
  "Azure_Resource_Group": {
    "display_name": "Azure Resource Group",
    "category": "Cloud",
    "description": "A logical container for Azure resources within a subscription.",
    "properties": ["name", "location", "subscription_id"],
    "required": ["name", "subscription_id"],
    "relationships": {
      "BELONGS_TO": {"target": "Azure_Subscription", "direction": "out"}
    }
  },
  "Azure_NSG": {
    "display_name": "Azure Network Security Group",
    "category": "Cloud",
    "description": "A security group that controls inbound and outbound traffic for Azure resources.",
    "properties": ["name", "nsg_id", "resource_group", "rules"],
    "required": ["name", "nsg_id", "resource_group"],
    "relationships": {
      "IN_RESOURCE_GROUP": {"target": "Azure_Resource_Group", "direction": "out"}
    }
  },
  "Azure_VNet": {
    "display_name": "Azure Virtual Network",
    "category": "Cloud",
    "description": "A logically isolated network in Azure.",
    "properties": ["name", "vnet_id", "address_space", "resource_group"],
    "required": ["name", "vnet_id", "resource_group"],
    "relationships": {
      "IN_RESOURCE_GROUP": {"target": "Azure_Resource_Group", "direction": "out"}
    }
  },
  "Azure_Subnet": {
    "display_name": "Azure Subnet",
    "category": "Cloud",
    "description": "A subnet within an Azure Virtual Network.",
    "properties": ["name", "subnet_id", "virtual_network"],
    "required": ["name", "subnet_id", "virtual_network"],
    "relationships": {
      "PART_OF_VNET": {"target": "Azure_Virtual_Network", "direction": "out"},
      "LINKED_TO_IPAM_NETWORK": {"target": "Network", "direction": "out"}
    }
  }
}
