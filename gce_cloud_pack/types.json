{
  "GCE_Project": {
    "display_name": "GCE Project",
    "category": "Cloud",
    "description": "A Google Cloud Project, the root container for GCP resources.",
    "properties": ["name", "project_id", "organization_id"],
    "required": ["name", "project_id"],
    "relationships": {}
  },
  "GCE_Network": {
    "display_name": "GCE Network",
    "category": "Cloud",
    "description": "A virtual network in Google Cloud Platform.",
    "properties": ["name", "network_id", "project_id", "cidr_block"],
    "required": ["name", "network_id", "project_id"],
    "relationships": {
      "BELONGS_TO": {"target": "GCE_Project", "direction": "out"}
    }
  },
  "GCE_Firewall_Rule": {
    "display_name": "GCE Firewall Rule",
    "category": "Cloud",
    "description": "A firewall rule that controls traffic for GCP resources.",
    "properties": ["name", "rule_id", "network_id", "direction", "priority", "action", "source_ranges", "target_tags"],
    "required": ["name", "rule_id", "network_id"],
    "relationships": {
      "IN_NETWORK": {"target": "GCE_Network", "direction": "out"}
    }
  },
  "GCE_Subnet": {
    "display_name": "GCE Subnet",
    "category": "Cloud",
    "description": "A subnet within a GCE Network.",
    "properties": ["name", "subnet_id", "network_id"],
    "required": ["name", "subnet_id", "network_id"],
    "relationships": {
      "PART_OF_NETWORK": {"target": "GCE_Network", "direction": "out"},
      "LINKED_TO_IPAM_NETWORK": {"target": "Network", "direction": "out"}
    }
  }
}
